# Supabase local environment variables
# Copy to .env and adjust as needed

# SMTP - set to false locally (emails go to Inbucket), true in production
RESEND_ENABLED=false

# Auth redirect URL
SITE_URL=http://localhost:5173
