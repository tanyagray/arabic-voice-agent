# Root environment variables — used by Supabase CLI locally.
# Copy to .env and fill in your values.
# See each service directory for service-specific env vars.

# Supabase CLI credentials (for `supabase link`, `supabase db push`, etc.)
SUPABASE_DB_PASSWORD=
SUPABASE_ACCESS_TOKEN=
